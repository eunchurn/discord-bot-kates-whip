apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kates-whip

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml

images:
  - name: ghcr.io/eunchurn/kates-whip
    newTag: latest
