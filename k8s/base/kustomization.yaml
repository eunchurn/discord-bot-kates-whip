apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ks-bot

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml

images:
  - name: ghcr.io/eunchurn/discord-bot-kates-whip
    newTag: latest
